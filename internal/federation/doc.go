// Package federation is intentionally dormant in Phase 1.
//
// Future phases may exchange signed archive packets between autonomous shards,
// but Phase 1 keeps each universe local and self-contained.
package federation
